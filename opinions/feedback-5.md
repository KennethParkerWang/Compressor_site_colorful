# 意见文档 5：甲方最终验收意见与乙方交付

## 甲方意见

1. 当前信息架构已经比上一版清晰，但仍需在交付说明中强调三页分工：SOTA、Datasets、Resource Library。
2. 文献库已经具备引用导出能力，但后续仍建议接入 Zotero 或 BibTeX 导入校验。
3. 数据集库数量明显提升，后续应补每个数据集的推荐实验协议和本地下载校验。
4. 实验台 baseline 已补全，后续应把命令模板从示例命令升级为按压缩器实际命令生成。
5. 算法模块页定位清楚，后续应把每个模块挂接到真实论文和源码位置。
6. 演化图可读性改善，后续仍建议增加主干/全量的图例说明和导出图片功能。
7. 算法目录已经支持多种排序，后续应补真实 benchmark 数据而不是只用定性 speed/ratio。
8. 本轮需要提交 git checkpoint，保证可回退。

## 乙方交付

1. 三页分工已落地：`/sota` 为榜单，`/datasets` 为数据集，`/hub` 为其他资源。
2. 文献库支持 GB/T 7714、IEEE、APA、BibTeX、RIS、CSL-JSON、Markdown 导出。
3. 数据集库按 8 类组织，并记录来源、用途、风险、许可和标签。
4. 实验台复用数据集库，并扩展 baseline。
5. 算法模块页扩展为 14 个架构模块。
6. 演化图新增深链接、视角重命名和画布间距优化。
7. 算法目录新增时间/分类/压缩率/速度排序。
8. 已完成构建和浏览器路由检查，准备提交最终 checkpoint。
